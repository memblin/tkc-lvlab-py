# tkc_lvlab.utils.passwords

Memorable password phrase generation and SHA-512-crypt hashing.
Standalone `createvm` uses this to give one-off VMs a usable
first-boot console password while keeping only the hash on disk.

::: tkc_lvlab.utils.passwords
